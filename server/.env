PORT=4000
# Database connection string (PostgreSQL)
# For Supabase: Get from Project Settings â†’ Database â†’ Connection string (URI)
# For local PostgreSQL: postgresql://USER:PASSWORD@localhost:5432/ciarleglio_db?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ciarleglio_db?schema=public"

# Supabase configuration (required for image storage)
# Get these from Supabase Dashboard â†’ Project Settings â†’ API
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"
SUPABASE_BUCKET="inventory-images"

# Admin authentication token (set a strong random value)
ADMIN_TOKEN="dev-admin-token-change-in-production"
